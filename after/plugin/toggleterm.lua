require("toggleterm").setup{
  open_mapping = [[<C-M-t>]], -- Ctrl + alt ((M)eta key) + t to toggle
  direction = 'vertical', -- can also be 'float' or 'horizontal'
  start_in_insert = true,
  size = 50,
}

